import { CharacterReader } from "../character-reader";

test("First test", () => {
  expect(true).toBe(true);
  // const testCase = "hmm\nhmm";
  // const cr = new CharacterReader(testCase);
  // const char = cr.getNextCharacter()
  // expect(char).toBe("h");
  // expect(cr.absolutePosition).toBe(1);
  // expect(cr.positionCol).toBe(1);
  // expect(cr.positionRow).toBe(0);
  // cr.getNextCharacter()
  // cr.getNextCharacter()
  // const char2 = cr.getNextCharacter();
  // expect(char2).toBe("\n");
  // expect(cr.absolutePosition).toBe(4);
  // expect(cr.positionCol).toBe(0);
  // expect(cr.positionRow).toBe(1);
  // cr.getNextCharacter()
  // cr.getNextCharacter()
  // cr.getNextCharacter()
  // cr.getNextCharacter()
  // cr.getNextCharacter()
  // const char3 = cr.getNextCharacter()
  // expect(char3).toBe(undefined);
});
